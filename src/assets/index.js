import { assetServerUrl } from "@/config";

export const assets = {
  searchIcon: assetServerUrl + "/search.svg",
  logo: assetServerUrl + "/logo.svg",
  hamburger: assetServerUrl + "/hamburger.svg"
};
